# Example environment configuration for kalacode

# ===== Azure OpenAI Configuration =====
# Use this for Azure OpenAI endpoints
OPENAI_API_KEY=your-azure-api-key-here
OPENAI_BASE_URL=https://your-resource.cognitiveservices.azure.com/openai/v1/
OPENAI_MODEL=gpt-4

# ===== Short-Term Memory Configuration =====
# Enable/disable short-term memory management
KALACODE_ENABLE_STM=true

# Maximum tokens to keep in context (prevents overflow)
KALACODE_MAX_CONTEXT_TOKENS=100000

# Maximum number of recent messages to keep
KALACODE_MAX_RECENT_MESSAGES=20

# ===== Long-Term Memory Configuration =====
# Persist memory in standalone markdown
KALACODE_ENABLE_LTM=true

# Markdown file path (relative to current working directory)
KALACODE_LTM_FILE=.kalacode_memory.md

# Max characters of LTM injected into system context
KALACODE_LTM_MAX_SUMMARY_CHARS=2000

# Max timestamped entries kept in markdown file
KALACODE_LTM_MAX_ENTRIES=500

# Fuzzy deduplication threshold (0.0 = off, 1.0 = exact match only)
KALACODE_LTM_DEDUP_THRESHOLD=0.82
